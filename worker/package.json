{
  "name": "sbarco-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "node --check src/index.js",
    "test": "node --test --test-isolation=none test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
